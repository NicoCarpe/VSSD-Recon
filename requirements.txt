# Training / evaluation dependencies (CUDA GPU required).
#
# torch is not pinned here: install the build matching your CUDA toolkit first,
# e.g.  pip install torch==2.5.1 --index-url https://download.pytorch.org/whl/cu124
#
# mamba_ssm provides the Triton chunk-scan kernel used by the causal SSD path
# (linear_attn_duality=False) and by the vss_recon baseline. The proposed
# VSSD-Recon configuration (linear_attn_duality=True) does not call it.

einops==0.8.1
fvcore==0.1.5.post20220512
h5py==3.11.0
jsonargparse[all]==4.39.0
lightning==2.4.0
lightning-utilities==0.11.6
mamba-ssm==2.2.4
matplotlib==3.10.0
numpy==1.26.4
PyYAML==6.0.2
scikit-image==0.24.0
scipy==1.15.1
timm==1.0.15
torchmetrics==1.4.1
wandb==0.19.6
